# AGENTS.md

## 开发约束
- 本 mod 无需考虑 Minecraft 版本迁移与存档/网络格式的向后兼容性：改动可以自由重构数据结构、
  序列化格式与包格式，不受旧版本/旧存档限制。

## 客户端 / 服务端数据一致性
- 服务端持有权威状态，客户端持有独立镜像；二者不得共享同一实例。
- 状态同步一律经由既定的网络协议完成，单机（集成服）也不例外；禁止以"同进程""本地连接"为由
  跳过协议或直接共享权威数据。
- 客户端的展示与交互只读取客户端镜像，禁止直读服务端权威状态。
- 客户端可保留不作为权威来源的本地草稿 / 预览以改善手感，但对外生效仍须通过协议提交到服务端。
- 追求单一代码路径：让单机运行覆盖专用服的结构性逻辑，避免按运行环境分叉行为。